# AGENTS

## Purpose
This repository is a very small workshop-style workspace with one C example and one Python example. There is no build system, no package manifest, and no CI configuration present.

## What to know
- `1.c` is a simple C console program that reads two integers and prints their sum.
- `testing.py` is a standalone Python script that prints a short message.
- `1.exe` and `tex.exe` are binary artifacts and should not be modified.

## AI agent guidance
- Prefer minimal, non-destructive changes aligned to the current user request.
- When modifying code, keep changes appropriate for a small demo/workshop repository.
- Do not add unrelated tooling, dependencies, or large infrastructure changes.
- If the user asks for new files, keep them lightweight and directly relevant to the C/Python examples.

## Notes
- No `README.md`, `CONTRIBUTING.md`, or `.github` customization files were found in this workspace.
